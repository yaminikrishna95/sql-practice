SELECT
    COALESCE(SUM(b.apple_count), 0) + COALESCE(SUM(c.apple_count), 0) AS apple_count,
    COALESCE(SUM(b.orange_count), 0) + COALESCE(SUM(c.orange_count), 0) AS orange_count
FROM Boxes b
LEFT JOIN Chests c
ON b.chest_id = c.chest_id